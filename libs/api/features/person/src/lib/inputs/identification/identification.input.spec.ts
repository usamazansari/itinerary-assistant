import { IdentificationInput } from './identification.input';

describe('IdentificationInput', () => {
  it('should be defined', () => {
    expect(new IdentificationInput()).toBeDefined();
  });
});
