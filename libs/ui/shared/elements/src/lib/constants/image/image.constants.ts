import type { ImageModel } from '../../models';

/**
 * Stub for `ImageModel`
 *
 * ```ts
 * const ImageStub: ImageModel = {
 *   src : '',
 *   alt : ''
 * }
 * ```
 */
export const ImageStub: ImageModel = {
  src: '',
  alt: ''
};
