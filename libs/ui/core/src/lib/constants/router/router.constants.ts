import type { RouterPayloadModel } from '../../models';

/**
 * Stub for `RouterPayloadModel`
 *
 * ```ts
 * const RouterPayloadStub: RouterPayloadModel = {
 *   routes : [],
 *   extras : {}
 * };
 * ```
 */
export const RouterPayloadStub: RouterPayloadModel = {
  routes: [],
  extras: {}
};
