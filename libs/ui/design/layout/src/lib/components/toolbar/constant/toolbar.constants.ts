import { Icon } from '../imports';
import { ToolbarAssets } from '..';

export const _ToolbarAssets: ToolbarAssets = new ToolbarAssets({
  trigger: new Icon({
    name: 'bars',
    style: 'fas'
  })
});
